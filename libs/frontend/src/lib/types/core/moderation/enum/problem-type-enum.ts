/**
 * Should be about things happening on the site, not the site itself, use github issues for that
 */
export const ProblemTypeEnum = {
	PUZZLE: "puzzle",
	USER: "user",
	COMMENT: "comment",
	GAME_CHAT: "game_chat"
} as const;
