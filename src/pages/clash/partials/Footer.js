import { Button, HStack } from "@chakra-ui/react";

const Footer = () => {
  return (
    <HStack>
      <Button>test</Button>
      <Button>attempt</Button>
    </HStack>
  );
};

export default Footer;
